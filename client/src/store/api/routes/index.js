export const baseUrl = "http://localhost:8080";

const search = {
    getSearchResults: '/api/search',
    getRecordSearchResults: 'api/search/records'
}

const ServerRoutes = {
    search
}

export default ServerRoutes;